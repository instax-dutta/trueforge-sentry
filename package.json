{
  "name": "sentry",
  "private": true,
  "version": "0.1.0",
  "description": "Licence-to-act incident responder on TrueForge",
  "license": "MIT",
  "engines": { "node": ">=22.14" },
  "scripts": {
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck"
  }
}
