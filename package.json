{
  "name": "sentry",
  "private": true,
  "version": "0.1.0",
  "description": "Licence-to-act incident responder on TrueForge",
  "license": "MIT",
  "engines": {
    "node": ">=22.14"
  },
  "scripts": {
    "test": "pnpm --filter @sentry/app test",
    "lint": "echo 'eslint config lands with dashboard PR'",
    "typecheck": "pnpm --filter @sentry/app typecheck"
  }
}
